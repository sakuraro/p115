# p115

## Install
```shell
git clone git@github.com:sakuraro/p115.git
cd p115
python -m venv .venv
source .venv/bin/activate
python -m pip install .
```
## Usage
```shell
python -m p115 upload -l "local file" -r "remote path"
```
