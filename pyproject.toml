[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "p115"
version = "2025.12.09"
requires-python = ">= 3.8"
dependencies = [
    "qrcode",
    "requests",
    "glom"
]
